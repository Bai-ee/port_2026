import { SettingsIcon } from 'bballi-portfolio';
export function Default() {
  return (
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '40px', background: '#fff' }}>
      <SettingsIcon size={64} />
    </div>
  );
}
